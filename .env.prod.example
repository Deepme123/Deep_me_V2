# Production template (external Postgres)
# Minimum boot requirement: DATABASE_URL

# Required (set external DB URL)
DATABASE_URL=postgresql+psycopg2://<USER>:<PASSWORD>@<HOST>:5432/<DB_NAME>

# DB policy (most managed Postgres providers require SSL)
DATABASE_DRIVER=psycopg2
DB_SSLMODE=require

# Runtime mode
ENV=prod
APP_ENV=prod

# Set frontend domains explicitly
CORS_ALLOW_ORIGINS=https://your-frontend.example.com

# Optional LLM
OPENAI_API_KEY=
LLM_MODEL=gpt-4o-mini
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=800

# Optional Google OAuth (required only for /auth/google* endpoints)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://your-api.example.com/auth/callback

# Recommended security overrides for production
JWT_SECRET_KEY=change-me
JWT_REFRESH_SECRET=change-me-too
COOKIE_SECURE=true
